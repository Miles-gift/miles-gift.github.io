# yoyo的旅行日记重建执行计划

> 文档性质：项目总纲、产品需求、设计规范、技术方案、实施路线与验收标准的单一事实来源（Single Source of Truth）
>
> 当前状态：阶段 5.1 信息架构与“流动手稿”视觉修订已完成，准备完成阶段 6 本地内容工作台与发布流程
>
> 文档版本：v1.9.1
>
> 最后更新：2026-09-10

> **V2 需求优先级说明（2026-09-10）**：本文件早期段落中关于 Miles 品牌、Apple 发布会风格、RSS、隐私/版权公开页、源码/上游公开入口、旧 URL 兼容跳转、独立友链页和“先摘要再发布学习笔记”的内容，均已被用户最新确认覆盖。当前唯一有效公开结构为“首页—文章—资料库—旅程—关于”；资料库发布本人授权的 Markdown 原文；视觉方向为温暖、手工感、青春的“流动手稿”；友联并入关于；RSS、隐私、版权、源码和旧兼容入口均不保留。完整视觉决策见 `doc/DESIGN_DIRECTION_V2.md`，累计实施状态见根目录 `README.md`。
>
> 目标仓库：`https://github.com/Miles-gift/miles-gift.github.io`
>
> 上游模板：`https://github.com/Motues/Momo`
>
> 模板基线：Momo `26.8.15`，导入提交 `24c37d2`
>
> 默认发布地址：`https://miles-gift.github.io/`

---

## 0. 如何使用本计划

本文件是后续开发的主依据。每次开始一项工作前，先定位到对应阶段；完成后勾选任务、记录决定、补充实际结果。若设计或技术方向发生变化，应先更新本文件，再修改代码，避免计划、代码和内容体系长期分叉。

执行约定：

- `[ ]` 表示未开始，`[~]` 表示进行中，`[x]` 表示已完成，`[!]` 表示受阻。
- 每一阶段只有在“退出标准”全部满足后才能进入下一阶段。
- P0 是首发必须完成，P1 是首发后优先增强，P2 是长期探索。
- 涉及个人信息、统计、评论或第三方服务时，必须先完成隐私审查。
- 不直接在 `main` 上进行大规模试验；功能开发使用 `codex/<feature>` 分支。
- 不把密码、令牌、身份证件、家庭住址、私人联系方式等提交到 Git。
- 本计划中的尺寸和颜色是首轮基线，必须经真实页面和移动端验证后再锁定。

每次开发的固定闭环：

1. 从本计划选取一个可独立验收的任务。
2. 明确涉及文件、依赖和验收条件。
3. 在功能分支实施。
4. 运行格式检查、类型检查、构建、页面与移动端验证。
5. 检查可访问性、性能和隐私影响。
6. 更新本计划与变更记录。
7. 合并到 `main`，由 GitHub Actions 自动部署。
8. 在线检查关键路径，必要时回滚。

---

## 1. 项目愿景与成功定义

### 1.1 一句话愿景

建设一个安静、清晰、具有舞台叙事感的个人数字花园：既能长期沉淀学习资料，也能容纳个人介绍、所思所想与人生经历，并允许内容以低摩擦方式持续发布和整理。

### 1.2 产品目标

1. **长期可写**：从产生想法到发布，常规文章不超过 5 分钟操作时间。
2. **长期可找**：内容可以通过栏目、主题、标签、年份、全文搜索和相关内容被重新发现。
3. **长期可维护**：正文以 Markdown 为主，不被某个封闭平台锁定；即使更换框架，内容也能迁移。
4. **建立个人表达**：视觉和文案体现个人气质，而不是停留在模板换名字的层面。
5. **阅读体验优先**：正文、图片、代码、数学公式和资料卡片都应清晰、克制、易读。
6. **发布可靠**：每次合并到 `main` 后自动构建与部署，失败时能快速定位和回滚。
7. **隐私可控**：公开内容、草稿和私人资料有明确边界，默认不采集不必要的数据。

### 1.3 非目标

首发阶段不做以下事项：

- 不建设社交网络、私信系统或复杂用户体系。
- 不提供任意访客上传文件的能力。
- 不追求重型动画、3D 场景或持续占用 GPU 的视觉特效。
- 不在首版同时维护完整中英双语内容；先保证中文体验完整。
- 不把博客变成网盘；大文件与版权受限资料只保存索引、摘要和合法来源链接。
- 不复制 Apple 商标、产品页面或受保护素材；只借鉴其克制、聚焦、舞台化的设计语言。

### 1.4 首发成功指标

| 维度 | 首发目标 | 检查方式 |
|---|---:|---|
| 可用性 | 首页、文章、资料、经历、关于、归档、搜索无阻塞错误 | 人工端到端检查 |
| 发布效率 | 新建并发布一篇普通文章 ≤ 5 分钟（不含写作） | 记录一次真实发布流程 |
| 构建可靠性 | `main` 连续 5 次构建成功 | GitHub Actions 记录 |
| 性能 | 首页移动端 Lighthouse Performance ≥ 90 | Chrome Lighthouse，至少测试 3 次 |
| 可访问性 | Lighthouse Accessibility ≥ 95；键盘可完成主要操作 | Lighthouse + 人工键盘检查 |
| SEO | 所有公开页面有唯一标题、描述、canonical、OG 图和 sitemap | 构建产物抽查 |
| 视觉一致性 | 核心页面全部使用统一令牌，无散落的临时颜色和尺寸 | CSS/组件审查 |
| 内容基线 | 至少 1 篇介绍、3 篇文章、5 条资料、3 条经历 | 内容清单 |

---

## 2. 当前技术基线与差距

### 2.1 已导入的 Momo 能力

- Astro 7 静态站点生成，Node.js 要求 `>= 22`，推荐 Node.js 24 LTS。
- Svelte 5 交互组件。
- Tailwind CSS 4 与 CSS 变量。
- Markdown 内容集合，支持草稿、分类、置顶、封面、目录、字数和阅读时间。
- Pagefind 本地全文搜索。
- RSS、深色模式、响应式布局与国际化基础。
- KaTeX、Typst、提示块、GitHub 卡片、音乐卡片、图片灯箱等富文本能力。
- 本地 CMS：文章增删改、实时预览、封面上传、分类统计与中英文版本管理。
- GitHub Actions → GitHub Pages 自动部署工作流。
- 可选评论系统：Momo Backend 或 Twikoo。

### 2.2 当前模板不能直接满足的需求

| 需求 | 当前状态 | 需要的改造 |
|---|---|---|
| 学习资料独立管理 | 只有通用博客分类 | 新建 `resources` 内容集合、筛选和资料详情 |
| 所思所想的短内容 | 只能作为普通文章 | 新建 `thoughts`/短记内容类型或统一内容类型字段 |
| 个人经历时间线 | 只有 About Markdown | 新建结构化 `journey` 集合和时间线页面 |
| 在线实时上传 | CMS 仅在本机运行 | 首发采用 Git 发布；后续增加鉴权的远程 Studio |
| Apple 发布会式表达 | 当前偏传统极简博客 | 重做设计令牌、首页叙事、卡片、字体、动效和媒体策略 |
| 个人品牌 | 阶段 1 已建立 Miles 名称、核心句、抽象标识、教育与成果摘要 | 阶段 2 完成设计系统，获得可公开影像与项目细节后继续个性化 |
| 内容治理 | 分类和字段较简单 | 建立 taxonomy、命名、状态、来源、复习日期和隐私等级 |
| 生产安全 | 评论已关闭且后端地址为空；模板统计脚本已移除 | 只在自有服务和隐私方案完成后重新启用 |

### 2.3 核心架构结论

站点继续采用“静态前台 + Git 内容库 + 自动构建”的架构。原因是成本低、速度快、安全边界清晰、内容易迁移，且与当前 GitHub Pages 仓库天然匹配。

需要正确理解“实时上传”：

- **本地实时预览**：本地 CMS 保存后，开发服务器立即热更新，接近即时。
- **线上自动发布**：内容提交并推送到 GitHub 后，Actions 构建并发布，通常是分钟级，不是数据库意义上的毫秒级实时。
- **随时随地在线编辑**：GitHub Pages 本身不能运行写入型 Node 后台。要实现这一点，需增加带登录鉴权的独立 Studio/服务，将内容安全地写回 GitHub，或改用 Headless CMS。

首发先完成可靠的本地 CMS → Git → GitHub Pages 工作流。远程 Studio 作为 P1，在站点内容模型稳定后再建设，避免过早引入账号、令牌和攻击面。

---

## 3. 用户、内容边界与表达原则

### 3.1 主要访问者

1. **未来的自己**：回顾学习脉络、想法演化和重要经历。
2. **同领域学习者**：查找结构化资料、笔记、实践结论和踩坑记录。
3. **朋友与合作方**：快速了解个人背景、兴趣、项目和联系方式。
4. **偶然访客**：从搜索引擎进入某一篇文章，能够继续理解站点结构。

### 3.2 内容层级

| 层级 | 定义 | 示例 | 发布规则 |
|---|---|---|---|
| Public | 可以永久公开和被搜索引擎收录 | 文章、学习笔记、公开经历 | 允许提交到仓库 |
| Unlisted | 可通过链接访问，但不出现在列表和 sitemap | 尚未正式发布的分享稿 | 必须有显式 `visibility` 字段 |
| Draft | 仅在本地预览 | 半成品文章、待核查资料 | `draft: true`，构建时排除 |
| Private | 不进入本仓库 | 日记、证件、敏感经历、私人资料 | 保存到加密的私人系统 |

### 3.3 隐私红线

公开内容默认不包含：

- 身份证件、账户凭据、API Token、Cookie、恢复码。
- 精确家庭住址、实时位置、日常固定路线。
- 未经同意的他人姓名、照片、联系方式或私密对话。
- 可反推出安全问题答案的信息。
- 无授权的付费课程、书籍、论文全文或受版权保护附件。
- 工作单位和项目中的机密、客户信息或内部系统截图。

### 3.4 写作语气

- 真诚、准确、克制，优先写清楚“我做了什么、为什么、结果怎样”。
- 对不确定内容标明时间、版本和置信程度。
- 学习资料必须尽可能附原始来源，不把二手总结伪装成原创结论。
- 不使用空泛口号堆砌个人形象；让项目、经历和文章本身建立可信度。
- 标题可有张力，但不使用与正文不符的夸张标题。

---

## 4. 信息架构与 URL 设计

### 4.1 顶层导航

首发导航建议控制在 5 个主入口：

1. `首页`：舞台式总览和内容入口。
2. `文章`：完整文章、教程、复盘与长篇思考。
3. `资料库`：学习资料、课程、书籍、工具和参考链接。
4. `旅程`：个人经历、阶段节点、项目与成长轨迹。
5. `关于`：个人介绍、关注领域、联系方法和站点说明。

辅助入口放入搜索、页脚或二级筛选：短想法、归档、标签、RSS、友链、隐私说明。

### 4.2 页面树

```text
/
├── /articles/                 文章列表
│   ├── /articles/[slug]/      文章详情
│   ├── /topics/[topic]/       主题聚合
│   └── /tags/[tag]/           标签聚合
├── /resources/                学习资料库
│   ├── /resources/[slug]/     资料详情/学习记录
│   └── /resources/?type=...   类型、状态、主题筛选
├── /thoughts/                 短想法/片段
├── /journey/                  个人经历时间线
├── /projects/                 项目案例（P1，可先并入旅程）
├── /archive/                  年份与月份归档
├── /about/                    关于我
├── /search/                   全站搜索
├── /privacy/                  隐私与版权说明
├── /rss.xml                   RSS
├── /sitemap-index.xml         Sitemap
└── /404/                      404 页面
```

如果为了降低首发复杂度暂不新增全部路由，可将 `thoughts` 作为文章类型，将 `projects` 合并到 `journey`；但内容字段必须从第一天预留，避免未来迁移困难。

### 4.3 首页叙事顺序

首页不是传统“最新文章列表”，而是一场可滚动的个人发布会。建议顺序：

1. **Opening / Hero**：一句身份主张 + 一句正在关注的事情 + 精选视觉。
2. **Now**：当前阶段、近期学习主题和正在做的项目。
3. **Featured Story**：一篇重点文章，以大画面和强标题呈现。
4. **Learning Library**：精选资料卡片，强调“可复用的知识”。
5. **Thoughts**：3–6 条短想法，节奏更轻。
6. **Journey**：3 个关键时间节点，引导进入完整经历页。
7. **Latest**：最新文章列表，保留博客的时间性。
8. **Closing**：个人签名式结束语、RSS 和联系入口。

首页每个屏幕只表达一个主要观点。禁止在首屏堆满标签、统计、按钮和轮播图。

### 4.4 分类体系

采用“内容类型 + 主题 + 标签”三层体系，避免把所有概念都塞进 category：

- **内容类型 `kind`**：article、note、thought、resource、journey、project。
- **主题 `topic`**：相对稳定的知识领域，如 Web、AI、阅读、研究方法、生活。
- **标签 `tags`**：更细的技术、书名、课程名、地点、事件等。
- **状态 `status`**：资源类专用，如 inbox、learning、completed、reference、archived。

规则：

- 每条内容只能有一个 `kind`。
- 每条公开内容建议有 1 个主 `topic`，最多 2 个。
- 标签通常为 2–5 个；禁止制造只出现一次且没有检索价值的同义标签。
- 标签使用统一中文或统一英文，不同时出现 `AI`、`人工智能`、`Artificial Intelligence` 三套同义标签。
- 每季度进行一次标签合并和孤立标签检查。

---

## 5. 内容模型与文件规范

### 5.1 长文章 `articles`

建议目录：`src/content/articles/<slug>/index.md`，同一文章的图片与附件就近存放。

```yaml
---
title: "文章标题"
slug: "stable-slug"
description: "80–160 字的独立摘要"
pubDate: 2026-09-09
updatedDate: 2026-09-09
draft: true
visibility: public
kind: article
topic: Web
tags: [Astro, 博客]
series: "个人网站重建"
seriesOrder: 1
cover: "./cover.webp"
coverAlt: "准确描述封面信息"
featured: false
pinTop: 0
lang: zh-cn
canonical: ""
license: CC-BY-NC-SA-4.0
---
```

字段规则：

- `slug` 发布后原则上不变；改名时保留重定向。
- `description` 不能机械截取正文第一段，应能独立说明文章价值。
- `updatedDate` 只在内容实质变化时更新，不因拼写修正刷新。
- `coverAlt` 必须描述图片表达的信息，装饰图使用空 alt。
- `canonical` 仅用于文章首发在其他平台或存在权威版本时。
- `featured` 用于首页重点内容，最多同时保持 3 条。

### 5.2 学习资料 `resources`

建议目录：`src/content/resources/<slug>.md`。资料卡既是收藏记录，也是个人学习结论，不能只存一个裸链接。

```yaml
---
title: "资料名称"
slug: "resource-slug"
resourceType: course
topic: AI
tags: [LLM, Prompting]
sourceUrl: "https://example.com/original"
author: "作者或机构"
publisher: "出版方/平台"
publishedYear: 2026
language: zh-cn
status: learning
progress: 40
rating: null
startedAt: 2026-09-01
completedAt: null
lastReviewedAt: 2026-09-09
cover: "./cover.webp"
description: "为什么值得看"
takeaways: ["核心收获一", "核心收获二"]
draft: true
visibility: public
---
```

`resourceType` 初始枚举：`book`、`course`、`paper`、`article`、`video`、`podcast`、`tool`、`repository`、`dataset`。

资料正文建议固定为：

1. 为什么收藏。
2. 适合谁。
3. 先备知识。
4. 核心内容。
5. 我的笔记与判断。
6. 可执行的下一步。
7. 来源与版权说明。

### 5.3 短想法 `thoughts`

短想法不是低质量文章。每条内容只表达一个观点，可在未来升级成长文。

```yaml
---
id: "20260909-001"
pubDate: 2026-09-09T20:30:00+08:00
topic: 生活
tags: [记录]
related: []
draft: true
visibility: public
---
```

规则：正文建议 30–500 字；超过 800 字或需要目录时转为文章；默认不使用封面；支持引用相关文章或资料。

### 5.4 经历 `journey`

```yaml
---
title: "阶段或事件名称"
slug: "journey-slug"
startDate: 2025-09-01
endDate: null
location: "可选，避免过度精确"
organization: "可选"
role: "可选"
kind: education
summary: "一句话说明这段经历"
highlights: ["成果或变化", "学到的事情"]
skills: ["技能一", "技能二"]
featured: false
order: 10
draft: true
visibility: public
---
```

`kind` 初始枚举：`education`、`work`、`project`、`life`、`milestone`。经历页强调选择、行动和反思，不公开敏感证明材料。

### 5.5 个人资料 `profile`

关于页的信息拆成可维护字段：

- 展示名、头像、短介绍、长介绍。
- 当前关注的 3–5 个方向。
- 技能与工具，但不做没有证据的“熟练度进度条”。
- 精选项目或作品。
- 教育/工作/生活经历入口。
- 联系方式：优先 GitHub、邮箱别名或公开社交链接。
- 可下载简历为 P1；发布前单独做隐私审查。

### 5.6 文件与媒体命名

- URL slug 使用小写英文、数字和连字符，如 `astro-content-model`。
- 图片名描述内容，如 `content-flow-diagram.webp`，不用 `IMG_1234.jpg`。
- 默认图片格式：照片用 AVIF/WebP，透明图优先 WebP/PNG，矢量图用 SVG。
- 单张普通内容图建议 ≤ 300 KB；首页 Hero 建议 ≤ 500 KB。
- 原始大图不直接进入仓库；保留一份外部备份，仓库只放站点优化版。
- 每张有意义的图片必须有 alt；图表还应在正文提供文字结论。
- 附件必须确认公开权限和版权，超过仓库合理体积时转对象存储。

### 5.7 内容生命周期

```text
Inbox → Draft → Review → Scheduled/Ready → Published → Updated → Archived
```

- Inbox：只记录线索，不承诺发布。
- Draft：形成结构和主要观点。
- Review：检查事实、链接、版权、隐私、排版与移动端效果。
- Ready：内容与元数据齐全，可以合并。
- Published：线上可访问并已验证。
- Updated：记录实质更新日期和变更说明。
- Archived：仍可访问但不再维护，页面明确提示。

---

## 6. 视觉设计系统：Apple 发布会式而非复制 Apple

### 6.1 设计关键词

`克制`、`聚焦`、`舞台感`、`大字叙事`、`高质量媒体`、`精准动效`、`宽阔留白`、`内容优先`。

设计判断顺序：

1. 这一屏的唯一重点是什么？
2. 文字层级是否在 3 秒内可辨认？
3. 图片是否提供信息或情绪，而非仅填空？
4. 动画是否帮助理解层级和状态？
5. 删除装饰后是否更清楚？若是，则删除。

### 6.2 色彩令牌基线

```css
:root {
  --color-bg: #ffffff;
  --color-bg-subtle: #f5f5f7;
  --color-surface: rgba(255, 255, 255, 0.72);
  --color-text: #1d1d1f;
  --color-text-secondary: #6e6e73;
  --color-divider: rgba(0, 0, 0, 0.10);
  --color-accent: #0071e3;
  --color-accent-hover: #0077ed;
  --color-focus: #0066cc;
  --color-success: #248a3d;
  --color-warning: #b25000;
  --color-danger: #d70015;
}

[data-theme="dark"] {
  --color-bg: #000000;
  --color-bg-subtle: #161617;
  --color-surface: rgba(29, 29, 31, 0.72);
  --color-text: #f5f5f7;
  --color-text-secondary: #a1a1a6;
  --color-divider: rgba(255, 255, 255, 0.14);
  --color-accent: #2997ff;
  --color-accent-hover: #64b5ff;
  --color-focus: #409cff;
}
```

要求：

- 正文、次要文字和交互控件都需通过 WCAG 对比度检查。
- 不用纯灰色表达错误或成功，必须结合文字、图标或形状。
- 同一页面最多有一个主强调色；图片本身可以丰富，但 UI 保持克制。
- 深色模式不是简单反色，图片、阴影、边框和代码块需分别调校。

### 6.3 字体系统

界面优先系统无衬线字体，正文根据阅读体验决定无衬线或高质量中文衬线：

```css
--font-ui: -apple-system, BlinkMacSystemFont, "SF Pro Display",
  "SF Pro Text", "PingFang SC", "Noto Sans SC", "Segoe UI", sans-serif;
--font-reading: "Noto Serif SC", "Source Han Serif SC", serif;
--font-mono: "JetBrains Mono", "SFMono-Regular", Consolas, monospace;
```

注意：不随站点分发无授权的 SF Pro 字体文件；Apple 设备自然使用系统字体，其他平台使用合法回退字体。

建议字号：

| 角色 | 桌面端 | 移动端 | 行高 | 字重 |
|---|---:|---:|---:|---:|
| Display Hero | 72–88 px | 44–52 px | 1.02–1.08 | 600–700 |
| Section Headline | 48–64 px | 34–42 px | 1.08–1.15 | 600–700 |
| Page Title | 40–56 px | 32–40 px | 1.10–1.18 | 600–700 |
| Card Title | 24–32 px | 22–28 px | 1.18–1.28 | 600 |
| Body | 18–20 px | 17–18 px | 1.7–1.85 | 400 |
| Metadata | 13–15 px | 13–14 px | 1.4–1.6 | 400–500 |

中文大标题慎用极端负字距；英数标题可使用约 `-0.02em` 至 `-0.04em`。正文行宽控制在约 38–44 个中文字符，不允许桌面端横跨全屏。

### 6.4 布局与间距

- 全局内容最大宽度：`1200px`；阅读正文最大宽度：`760px`。
- 横向安全边距：桌面 `32–48px`，平板 `24–32px`，手机 `20px`。
- 使用 4px 基准间距；常用值：4、8、12、16、24、32、48、64、96、128。
- 大段落之间优先用 96–160px 的垂直留白形成舞台章节。
- 圆角基线：小控件 10–12px，卡片 20–28px，Hero 媒体 28–36px。
- 卡片阴影极轻，优先靠背景层级和边界区分；深色页面减少外发光。
- 导航使用半透明材质时，必须提供不支持 `backdrop-filter` 的纯色回退。

### 6.5 动效规范

动效用于“揭示、定位、反馈”，不用于炫技：

- 微交互：120–220ms，用于 hover、按钮按下、图标状态。
- 页面组件：320–500ms，用于卡片进入、筛选切换、抽屉开合。
- 舞台章节：600–900ms，用于重点标题和媒体揭示。
- 缓动优先 `cubic-bezier(0.22, 1, 0.36, 1)`；避免所有元素同时弹跳。
- 首屏只做一次轻量进入动画；返回页面时不强制重复长动画。
- 滚动动画采用 IntersectionObserver，禁止逐帧监听制造卡顿。
- 必须支持 `prefers-reduced-motion: reduce`，关闭位移、缩放和视差。
- 不使用自动播放有声视频；视频应提供封面、字幕和暂停控制。

### 6.6 核心组件外观

**顶部导航**

- 桌面为居中胶囊式导航或通栏玻璃导航，页面滚动后轻微收紧。
- 当前栏目必须有明确但克制的选中状态。
- 搜索、主题切换用图标按钮并提供 `aria-label` 和可见焦点。
- 手机端使用全屏或底部抽屉，触控目标至少 44×44px。

**Hero**

- 首屏大标题最多两行，副标题最多三行。
- 主要 CTA 不超过两个，如“查看文章”和“认识我”。
- 背景可使用抽象渐变、个人摄影或作品视觉，不用廉价图库式人物图。
- 媒体加载前必须预留尺寸，避免布局跳动。

**文章卡片**

- 首页精选卡使用大媒体；普通列表使用紧凑文字卡或小缩略图。
- 信息顺序固定：主题/类型 → 标题 → 摘要 → 日期/阅读时间。
- 整卡可点击，但标题仍保留语义链接和清晰焦点态。

**资料卡片**

- 突出资料类型、学习状态、个人评价和来源。
- 进度只在确实维护时展示；不制造虚假的精确度。
- 外链明显标识，并在新标签打开时提供可理解提示。

**时间线**

- 桌面端可使用左右交替或纵向大章节；移动端统一单列。
- 每一节点突出“发生了什么、产生了什么变化”，不是简历条目堆砌。

**文章页**

- 标题、摘要、日期、更新时间、主题、阅读时间在首屏形成清晰层级。
- 正文目录在大屏可粘滞，小屏折叠；当前章节需高亮。
- 代码块有语言标识、复制按钮、横向滚动和清晰焦点。
- 文末显示相关文章、更新时间、许可协议、反馈或评论入口。

---

## 7. 页面级重建设计

### 7.1 首页 P0

任务：

- [x] 确定站点中文名、英文名或个人署名。
- [x] 完成 Hero 主标题、说明文字和两个 CTA。
- [x] 建立 6–8 个舞台章节，移动端压缩为自然单列。
- [x] 制作原创“信号轨迹”抽象视觉。
- [x] 接入精选文章、资料、想法、经历和最新内容的数据查询。
- [x] 首屏使用有明确尺寸的本地 SVG；正文媒体按需加载，不引入无必要位图。
- [x] 为一次性首屏动画实现 reduced-motion 回退，取消批量滚动淡入。
- [x] 在 360、390、768、1024px 与当前浏览器可用的 1280px 宽度检查布局。
- [ ] 在阶段 7 使用可设定 1440px 的浏览器环境补做桌面宽屏检查。

验收：首屏 3 秒内能回答“这是谁、这里有什么、下一步去哪”；手机端首屏无横向滚动；首屏关键内容不依赖动画才可见。

### 7.2 文章列表与详情 P0

- [x] 从当前 Momo 博客列表迁移到新的卡片层级。
- [x] 支持主题、标签、年份筛选。
- [x] 筛选状态进入 URL query，刷新和分享后可恢复。
- [x] 空状态提供返回全部内容的操作。
- [x] 文章详情支持目录、脚注、代码、公式、图片、引用和提示块。
- [x] 增加更新时间与“本文是否仍维护”的标识。
- [x] 增加系列上一篇/下一篇和相关推荐。
- [x] 统一外链样式并标识外部跳转。

验收：一篇 3000 字、含代码/公式/多图的样例文章在桌面与手机均可舒适阅读。

### 7.3 资料库 P0

- [x] 新建 `resources` 内容集合与 schema。
- [x] 建立类型、主题、状态三个筛选维度。
- [x] 提供关键词搜索；与 Pagefind 全站搜索互补。
- [x] 卡片显示资料类型、来源、状态、短评和更新时间。
- [x] 资料详情展示学习笔记、关键收获和原始来源。
- [x] 对失效链接增加人工复查字段和归档状态。
- [x] 明确附件版权，不镜像无授权内容。

验收：可在 3 次交互内找到“正在学习的 AI 课程”或“已完成的某主题书籍”。

### 7.4 短想法 P0/P1

- [x] 建立独立 `thoughts` 轻量内容类型与详情。
- [x] 列表按时间倒序，减少封面和复杂元数据。
- [x] 支持关联文章、资料和标签。
- [ ] P1 提供独立快速发布表单。
- [ ] P1 支持按月折叠和 RSS 独立订阅。

验收：发布一条短想法只需填写正文、主题和可选标签。

### 7.5 旅程 P0

- [x] 新建经历 schema 与时间线数据查询。
- [x] 设计“阶段—事件—反思”三级信息层次。
- [x] 允许公开经历正文引用相关文章或项目。
- [x] 对空档期、地点和组织信息保持可选，不强迫暴露隐私。
- [x] 提供打印友好样式，但不等同于公开简历。

验收：访客能在 2 分钟内理解主要阶段、兴趣变化和代表性成果。

### 7.6 关于页 P0

- [x] 替换模板作者头像、姓名、介绍和站外链接（阶段 1 已完成身份基线）。
- [x] 撰写短介绍与长介绍基线（阶段 1 已完成，得到真实经历后继续扩充）。
- [x] 展示当前关注、做事方式、精选内容和联系方式。
- [x] 增加“关于本站”：技术栈、内容许可、更新方式。
- [x] 首发不放邮箱，只保留本人确认的 GitHub 联系入口。

验收：不依赖简历附件即可建立清晰、可信、不过度包装的个人形象。

### 7.7 搜索与归档 P0

- [x] Pagefind 索引所有公开内容，排除草稿、私密和系统页面。
- [x] 搜索结果标识内容类型、主题、日期和摘要。
- [x] 中文查询、英文技术词、标题、正文和标签均能命中。
- [x] 归档按年份分组，并提供类型/主题过滤。
- [x] 搜索弹窗支持键盘打开、关闭、上下选择和回车跳转。

验收：使用 10 个预设查询测试，相关内容位于前 5 条；无结果时提供替代建议。

### 7.8 404、隐私与页脚 P0

- [x] 404 页面保持品牌风格，提供首页、搜索和文章入口。
- [x] 隐私页说明使用的托管、统计、评论、字体和第三方嵌入边界。
- [x] 页脚展示版权年份、许可、RSS、GitHub 和必要链接。
- [x] 当前公开内容不自动加载外部音乐或视频；未来嵌入继续遵守点击后加载原则。

---

## 8. 代码结构与具体改造地图

### 8.1 保留并重构的现有文件

| 文件/目录 | 计划 |
|---|---|
| `astro.config.mjs` | 修改站点 URL、i18n、Markdown、sitemap 和安全相关配置 |
| `src/config.ts` | 替换模板品牌信息，逐步拆分为 site/profile/social/navigation 配置 |
| `src/content.config.ts` | 扩展 articles/resources/thoughts/journey 等 schema |
| `src/components/Header.astro` | 重建 Apple 式响应式导航、焦点态和菜单行为 |
| `src/components/Cover.astro` | 演进为可复用 Hero/SectionIntro，而非所有页面统一标题块 |
| `src/components/PostCard.astro` | 拆成 FeaturedStory、ArticleCard、CompactItem 等明确变体 |
| `src/styles/variables.css` | 升级为完整设计令牌，兼容深浅主题 |
| `src/styles/global.css` | 重设字体、基础排版、焦点、容器和 reduced-motion |
| `src/styles/markdown.css` | 建立长文阅读系统与富内容组件规范 |
| `src/content/blog/` | 清理模板示例并迁移到新的真实内容结构 |
| `src/content/spec/` | 重写 About/Friends；评估是否保留 friends |
| `cms/` | 在内容模型稳定后扩展资料、想法和经历的编辑能力 |
| `.github/workflows/deploy.yml` | 增加校验、构建缓存、并发控制和部署验收 |

### 8.2 建议新增的结构

```text
src/
├── components/
│   ├── home/
│   │   ├── HomeHero.astro
│   │   ├── NowSection.astro
│   │   ├── FeaturedStory.astro
│   │   ├── LearningShelf.astro
│   │   ├── ThoughtStream.astro
│   │   └── JourneyPreview.astro
│   ├── content/
│   │   ├── ArticleCard.astro
│   │   ├── ResourceCard.astro
│   │   ├── ThoughtItem.astro
│   │   ├── JourneyItem.astro
│   │   └── ContentMeta.astro
│   ├── layout/
│   │   ├── SiteHeader.astro
│   │   ├── SiteFooter.astro
│   │   ├── PageHero.astro
│   │   └── Section.astro
│   └── ui/
│       ├── Button.astro
│       ├── Chip.astro
│       ├── IconButton.astro
│       ├── EmptyState.astro
│       └── ExternalLink.astro
├── content/
│   ├── articles/
│   ├── resources/
│   ├── thoughts/
│   ├── journey/
│   └── pages/
├── pages/
│   ├── articles/
│   ├── resources/
│   ├── thoughts/
│   ├── journey/
│   ├── topics/
│   ├── tags/
│   └── privacy.astro
├── styles/
│   ├── tokens.css
│   ├── base.css
│   ├── prose.css
│   ├── utilities.css
│   └── motion.css
└── utils/
    ├── content.ts
    ├── seo.ts
    ├── taxonomy.ts
    └── images.ts
```

### 8.3 组件原则

- 一个组件应有明确职责，不创建仅包装一层 `div` 的无意义组件。
- 页面级组件负责叙事，UI 组件负责一致性，内容查询留在 utils 或页面层。
- Astro 优先；只有真正需要客户端状态和交互时才使用 Svelte hydration。
- 默认使用服务端/构建期输出，减少发送给浏览器的 JavaScript。
- 所有变体通过明确 prop 或数据模型表达，不用页面内复制粘贴。
- 设计令牌集中管理；禁止在多个组件散落相近但不一致的颜色和圆角。

---

## 9. 上传、编辑与发布工作流

### 9.1 P0：本地 CMS + Git 自动部署（推荐首发方案）

作者流程：

1. `git pull --ff-only` 获取最新内容。
2. `pnpm install`（依赖变更时）或直接启动现有环境。
3. 运行 `pnpm dev` 查看站点。
4. 运行 `pnpm cms` 打开本地管理后台。
5. 新建文章/资料/想法/经历，上传优化后的图片。
6. 保存为草稿并在桌面、手机尺寸预览。
7. 完成发布检查，改为公开状态。
8. 查看 `git diff`，确认无敏感内容和意外大文件。
9. 提交并推送到功能分支。
10. 自动检查通过后合并 `main`。
11. GitHub Actions 构建并发布。
12. 打开线上 URL 验证正文、图片、搜索和分享卡片。

CMS 改造任务：

- [ ] 为不同内容类型提供独立表单和字段校验。
- [ ] 自动生成 slug，但允许发布前修改。
- [ ] 上传前检查文件类型、尺寸和文件名。
- [ ] 自动生成 WebP/AVIF 与多尺寸图片。
- [ ] 保存前提示缺失的摘要、alt、来源、标签和日期。
- [ ] 提供预发布检查面板。
- [ ] 删除继续保留二次确认，并优先进入可恢复的回收区。
- [ ] 支持内容间关联选择，而非手写易错路径。
- [ ] CMS 仅监听 `localhost`，不直接暴露公网。

### 9.2 P1：远程 Studio

目标：在经过身份验证后，从任意设备编辑内容、上传图片并触发部署。

首选方案评估：

| 方案 | 优点 | 风险/成本 | 适用阶段 |
|---|---|---|---|
| GitHub Web 编辑 | 零额外开发、审计清晰 | 移动端体验一般、媒体管理弱 | 立即可用的备用方案 |
| GitHub Codespaces | 环境一致、可运行 CMS | 启动较重、存在额度和成本 | P0/P1 过渡 |
| Decap CMS + OAuth | Git-based、生态成熟 | 需配置 OAuth/代理，界面需适配 | 内容模型稳定后的快捷方案 |
| 自建 Studio + Cloudflare Worker/GitHub App | 体验和权限可控 | 开发与安全维护成本最高 | P1/P2 |
| Headless CMS | 在线编辑成熟、媒体管理强 | 平台依赖、迁移和费用 | 内容团队扩大后再评估 |

决策门槛：当每月发布 ≥ 8 次，或明确需要手机端发布时，再启动远程 Studio。否则本地 CMS 足够且更安全。

远程 Studio 安全要求：

- 使用 OAuth/GitHub App，不把长期个人访问令牌放在浏览器。
- 最小权限，只允许操作指定仓库和内容目录。
- 登录回调、CSRF、会话过期、速率限制、上传 MIME 检查必须完成。
- 所有写入形成 Git 提交，保留作者、时间和变更记录。
- 上传限制大小和类型；SVG 默认拒绝或严格净化。
- 后台不与公开站点共用无保护的写入接口。

### 9.3 媒体与大文件策略

- P0：优化后的图片跟随文章进入 Git，简单可靠。
- 当仓库接近 500 MB 或单次媒体明显增多时评估 Cloudflare R2/对象存储。
- 视频不直接提交 Git；使用可信视频平台或对象存储，页面提供封面、字幕和降级链接。
- PDF 只发布有权公开的版本，并同时提供内容摘要和文件大小。
- 每季度检查仓库体积、最大文件、孤立媒体和失效引用。

---

## 10. SEO、分享与内容发现

### 10.1 技术 SEO P0

- [ ] 每页唯一 `<title>`，建议 30–60 个中文字符以内。
- [ ] 每页唯一 description，清晰概括内容而非关键词堆砌。
- [ ] 设置 `canonical`，避免语言和分页重复。
- [ ] 生成 sitemap，只包含公开、可索引页面。
- [ ] 生成 robots.txt，并明确不索引 Studio、草稿和预览。
- [ ] 增加 Open Graph 和 Twitter/X Card 元数据。
- [ ] 为文章提供 Article JSON-LD，为个人信息提供 Person JSON-LD。
- [ ] 文章图片包含宽高，分享图统一为 1200×630。
- [ ] URL 改动建立重定向表，不让已发布链接失效。
- [ ] RSS 包含正确的绝对 URL、发布日期、摘要和语言。

### 10.2 站内发现 P0

- 首页精选由人工策展，不只看发布时间。
- 每篇内容至少提供 2 条相关内容，优先同主题，其次共享标签。
- 资料与文章双向关联：文章引用资料，资料页列出实践文章。
- 经历可关联项目与文章，形成个人成长网络。
- 页面底部提供“继续探索”，不使用无限滚动。

### 10.3 统计策略 P1

首发可以不接统计。若接入，优先选择隐私友好、无 Cookie 或最小化 Cookie 的方案，并在隐私页披露。

只收集能驱动决策的数据：

- 页面访问趋势和来源域名。
- 搜索词是否命中。
- 主要内容入口的点击。
- 404 路径与外链失效。

不建立个人访客画像，不采集不必要的精确位置和跨站行为。

---

## 11. 性能预算

| 指标 | P0 预算 | 说明 |
|---|---:|---|
| 首屏 JS（gzip） | ≤ 80 KB | Astro 页面尽量零 JS，交互按需 hydration |
| 单页 CSS（gzip） | ≤ 50 KB | 清理未使用规则和重复样式 |
| 首页首屏媒体 | ≤ 700 KB | 移动端应更低 |
| LCP | ≤ 2.5 s | 模拟中端手机与较慢网络 |
| CLS | ≤ 0.1 | 图片、字体和动态区域预留空间 |
| INP | ≤ 200 ms | 搜索、菜单、筛选保持即时反馈 |
| Lighthouse Performance | ≥ 90 | 移动端生产构建 |

性能任务：

- [ ] 自托管必要字体并做子集化，或直接使用系统字体减少请求。
- [ ] Hero 图片预加载但只预加载实际首屏资源。
- [ ] 其他图片懒加载，生成响应式 `srcset`。
- [ ] 避免布局期间读取/写入交错造成强制重排。
- [ ] Pagefind 搜索脚本只在打开搜索时加载。
- [ ] 评论、视频、音乐等第三方组件点击后加载。
- [ ] 检查每个 Svelte island 是否确实需要 hydration。
- [ ] Actions 构建记录产物体积，明显回归时阻止合并。

---

## 12. 可访问性标准

- 全站使用语义化 `header`、`nav`、`main`、`article`、`aside`、`footer`。
- 每页只有一个主 `h1`，标题层级不跳级。
- 所有功能可用键盘完成；焦点顺序符合视觉顺序。
- 焦点样式清晰可见，不以 `outline: none` 直接移除。
- 触控目标至少 44×44px，密集标签仍保留足够间距。
- 颜色不是唯一信息载体；错误、状态和选中同时用文字/图标表达。
- 图片提供恰当 alt；装饰图片使用空 alt。
- 表单有可见 label、错误说明和 `aria-describedby`。
- 搜索弹窗、菜单和对话框处理焦点锁定、Esc 关闭与关闭后焦点返回。
- 动态结果通过适当 live region 告知，但不频繁打扰。
- `lang` 与内容语言一致；中英文混排时必要处设置局部语言。
- 支持 200% 页面缩放，不丢内容、不横向滚动。
- 支持 reduced motion、高对比和系统深色偏好。

验收设备至少包括：仅键盘、VoiceOver 或 NVDA 一次主流程、浏览器 200% 缩放、减少动态效果模式。

---

## 13. 安全、隐私与版权

### 13.1 P0 安全任务

- [ ] 首发前关闭模板默认的 `https://api-momo.motues.top` 评论后端。
- [ ] 若暂不部署自有评论，设置 `comments.enable: false`。
- [ ] 检查所有外部脚本和嵌入来源，删除不必要项。
- [ ] GitHub Actions 依赖固定到可信主版本，定期更新。
- [ ] 配置 Dependabot 或等价依赖更新机制。
- [ ] 构建时扫描意外提交的密钥和超大文件。
- [ ] 对用户可控 Markdown HTML 做安全边界审查。
- [ ] 外链使用合适的 `rel="noopener noreferrer"`。
- [ ] 若未来加入表单，后端必须有验证、限流和垃圾内容防护。

### 13.2 评论策略

评论属于 P1。启用前需决定：

- 是否真的需要评论，还是使用邮件/GitHub Discussion 反馈足够。
- 数据存放在哪里、保留多久、谁能删除。
- 是否允许匿名评论。
- 如何处理垃圾内容、违法内容和他人隐私。
- 第三方服务会设置什么 Cookie、收集什么数据。

推荐顺序：无评论首发 → GitHub Discussions 或邮件反馈 → 有明确需求再部署自有 Twikoo/Momo Backend。

### 13.3 版权策略

- 明确网站代码与内容使用不同许可：模板代码遵循上游 LICENSE；个人原创内容可选择 CC BY-NC-SA 4.0 或保留所有权利。
- 引用遵守合理范围并链接原始来源。
- 图片、字体、音乐、视频和图标都记录许可来源。
- 学习资料页存自己的摘要和笔记，不复制受版权保护全文。
- AI 生成媒体如被使用，应保留生成记录并按需要披露。

---

## 14. Git、CI/CD 与上游同步

### 14.1 远程仓库职责

- `origin`：个人站点仓库，日常 push/pull 和发布来源。
- `upstream`：Momo 模板，只用于获取上游更新，不直接推送。

### 14.2 分支和提交

- `main`：始终保持可构建、可部署。
- `codex/design-system`：视觉系统。
- `codex/content-model`：内容 schema 与迁移。
- `codex/homepage`：首页重建。
- `codex/resources`：资料库。
- `codex/cms-content-types`：CMS 扩展。

提交建议使用简洁的语义前缀：

- `feat:` 新功能。
- `fix:` 修复。
- `design:` 视觉或交互调整。
- `content:` 内容变化。
- `docs:` 文档。
- `refactor:` 不改变功能的重构。
- `chore:` 工具与依赖维护。

### 14.3 CI 流水线 P0

Pull Request：

1. 安装锁定依赖 `pnpm install --frozen-lockfile`。
2. 类型/配置检查。
3. CMS 冒烟测试（涉及 CMS 时）。
4. 生产构建。
5. 检查断链、重复 slug、缺失 alt 和无效 frontmatter。
6. 可选生成预览站点。

`main`：

1. 重复关键校验。
2. 构建 Astro 与 Pagefind 索引。
3. 上传 GitHub Pages artifact。
4. 部署。
5. 请求首页、文章样例、搜索索引和 RSS，执行部署后冒烟检查。

工作流要求：

- 使用 concurrency，新的部署取消同分支旧任务。
- 最小化 GitHub Actions permissions。
- 构建失败绝不发布半成品。
- 保留最近成功版本，可通过回滚提交恢复。

### 14.4 同步 Momo 上游

每月或有明确安全修复时检查一次：

```bash
git fetch upstream
git log --oneline --left-right main...upstream/main
```

同步原则：

- 不直接整体覆盖个性化代码。
- 先阅读 release 和 diff，按功能挑选或在独立分支合并。
- 优先同步安全修复、框架兼容和严重 bug；视觉层更新通常手工迁移。
- 每次同步前创建分支，完成构建和页面回归后再合并。
- 在本文件变更记录中记载同步的上游提交。

---

## 15. 测试矩阵与验收清单

### 15.1 自动检查

- [ ] TypeScript/Astro 检查通过。
- [ ] `pnpm build` 成功。
- [ ] Pagefind 索引成功且结果非空。
- [ ] 所有内容 schema 校验通过。
- [ ] 不存在重复 slug。
- [ ] 内部链接无 404。
- [ ] sitemap、RSS、robots 可生成。
- [ ] CMS 冒烟测试通过。
- [ ] 没有意外提交密钥或超大文件。

### 15.2 浏览器与设备

| 平台 | 浏览器 | P0 检查 |
|---|---|---|
| macOS | Safari 最新版 | 全流程、字体、毛玻璃、深色模式 |
| macOS/Windows | Chrome 最新版 | 全流程、Lighthouse、搜索 |
| Windows | Edge 最新版 | 基础回归 |
| iPhone | Safari | 导航、阅读、图片、筛选、旋转 |
| Android | Chrome | 导航、阅读、搜索、触控 |

响应式断点重点：320、360、390、430、768、1024、1280、1440、1920px。不是只在断点尺寸检查，还需拖动观察断点之间是否突变。

### 15.3 核心用户流程

1. 首页 → 精选文章 → 阅读 → 相关文章。
2. 首页 → 资料库 → 筛选 → 资料详情 → 原始来源。
3. 搜索按钮 → 输入中文关键词 → 键盘选择 → 结果页面。
4. 旅程 → 某一经历 → 关联项目/文章。
5. 关于 → 联系入口。
6. 深色模式切换 → 刷新 → 偏好保持。
7. 直接访问深层 URL → 页面正常 → 返回导航明确。
8. 不存在的 URL → 404 → 搜索或返回首页。
9. CMS 新建草稿 → 预览 → 上传图 → 保存 → 发布 → 线上验证。

### 15.4 内容发布前检查

- [ ] 标题准确，slug 稳定。
- [ ] 摘要能独立表达价值。
- [ ] 日期、主题、标签、类型正确。
- [ ] 所有事实、引用和链接已核查。
- [ ] 图片已压缩，alt 和尺寸齐全。
- [ ] 无私人信息、密钥和版权问题。
- [ ] 桌面和手机预览通过。
- [ ] 深浅主题通过。
- [ ] 搜索结果摘要合理。
- [ ] 分享卡片预览正确。
- [ ] 草稿标识已按预期切换。

### 15.5 全站 Definition of Done

只有满足以下条件才能称为首发完成：

- [x] 模板作者信息不再被当作本站身份，示例文章和默认后端全部退出生产（允许在页脚和文档中保留透明的上游归属链接）。
- [ ] 新信息架构和核心页面全部上线。
- [ ] 内容类型与 CMS 工作流可以支持日常使用。
- [ ] 首发内容达到最低数量且不是占位文案。
- [ ] 自动构建、部署和部署后验证稳定。
- [ ] 性能、可访问性和 SEO 达到本计划基线。
- [ ] 手机端经过真机检查。
- [ ] 隐私页和版权说明上线。
- [ ] 已记录回滚方式和日常发布 SOP。

---

## 16. 分阶段实施路线

### 阶段 0：基线固化（0.5–1 天，P0）

目标：确保模板是一个可构建、可回退的干净起点。

- [x] 将 Momo 模板源码导入个人仓库工作区。
- [x] 保留个人仓库为 `origin`。
- [x] 添加 Momo 为 `upstream`。
- [x] 固化 Node.js 24 LTS 与 pnpm 11 环境要求；本机 Node.js 26 兼容构建通过，CI 使用 Node.js 24。
- [x] 安装依赖并运行 `pnpm build`。
- [x] 运行模板站点并保存桌面/移动端基线截图。
- [x] 检查 GitHub Pages 仓库设置并由 GitHub Actions 完成基线发布。
- [x] 提交“模板基线”独立提交，方便未来比较。

退出标准：本地生产构建成功；模板基线提交可随时恢复；远程发布链路可用。

### 阶段 1：个人品牌与内容盘点（1–2 天，P0）—已完成

需要输入：

- [x] 站点名称和署名：首版统一使用 `Miles`。
- [x] 一句话介绍、长介绍：核心句已落地，About 已改为真实且不虚构履历的站点介绍。
- [x] 头像或代表性视觉：未授权公开照片，首版使用自有抽象字母 M 标识。
- [x] 当前关注的 3–5 个方向：电子设计与嵌入式、AI 与机器学习、工程工具、语言学习、长期知识整理。
- [x] 公开联系方式：当前仅公开 `github.com/Miles-gift`。
- [x] 已有文章、笔记、课程记录和经历素材清单：已盘点授权的本地目录，记录可转化内容、版权风险与隐私等级。
- [x] 明确暂不公开的内容：已建立 Public / Draft / Private 边界和发布前隐私检查表。

产出：[品牌与内容简报](doc/BRAND_AND_CONTENT_BRIEF.md)、[个人素材盘点与发布边界](doc/PERSONAL_CONTENT_INVENTORY.md)、栏目命名、首发内容列表、首篇建站记录。

退出标准：已通过。关于页已包含用户明确提供的教育经历、经材料核验的三项脱敏成果、真实关注方向和公开边界；原始证明、学号、证书编号与第三方身份没有进入仓库；Momo 示例资料已移入 `doc/template-content/` 并退出生产内容集；站点以《从这里开始》作为首篇内容。

### 阶段 2：设计系统（2–4 天，P0）

- [x] 将颜色、字体、间距、圆角、阴影、层级、动效写成令牌。
- [x] 建立 Button、Chip、IconButton、Section、PageHero 基础组件。
- [x] 重做全局排版、焦点、选择色、滚动条和 reduced-motion。
- [x] 重做顶部导航与页脚。
- [x] 创建设计验证页，覆盖全部控件状态和正文元素。
- [x] 深浅主题分别检查。

退出标准：已通过。语义令牌、基础组件和 `/design-system/` 验证页均已落地；桌面/移动、深浅主题、菜单 Esc 与焦点返回通过浏览器检查；新页面可以直接组合现有令牌和组件。

### 阶段 3：内容模型与迁移（2–4 天，P0）—已完成

- [x] 确认 collections 数量和字段。
- [x] 使用 Zod 严格校验日期、枚举、URL 和必填字段。
- [x] 建立 taxonomy 工具与重复 slug 检查。
- [x] 写入每种内容的示例文件。
- [x] 将需要保留的 Momo 示例移到内部参考或删除（阶段 1 已提前完成）。
- [x] 为旧 URL 规划重定向。
- [x] 修改 RSS 和搜索索引覆盖全部公开类型。

退出标准：已通过。`articles`、`resources`、`thoughts`、`journey` 和 `pages` 均有严格 schema 与真实样例；构建前会检查受控主题、路径标识、重复 URL 和高风险附件；生产构建生成 26 个页面，Pagefind 索引 19 个中文页面，RSS 输出 16 条公开内容；旧 `/blog/` URL 保留兼容迁移页。

### 阶段 4：首页与主导航重建（3–5 天，P0）—已完成

- [x] 完成首页线框和移动端顺序。
- [x] 完成 Hero 与主视觉。
- [x] 接入 Now、精选文章、资料、想法、旅程和最新内容。
- [x] 添加节制的滚动揭示。
- [x] 优化首屏图片与字体加载。
- [x] 完成导航、移动菜单和键盘交互。

退出标准：已通过首屏信息层级、桌面与 390×844 移动端检查；首页只有一个 H1、无水平溢出，五项主导航全部可达，菜单状态、Esc 与焦点返回正确。首页不增加远程字体、第三方脚本或大尺寸图片；正式 Lighthouse 数值随阶段 7 全站统一验收。

### 阶段 5：内容页面（4–7 天，P0）—已完成

- [x] 文章列表与详情。
- [x] 资料库列表、筛选与详情。
- [x] 短想法列表。
- [x] 经历时间线。
- [x] 关于、归档、标签/主题聚合。
- [x] 404、隐私和版权页面。
- [x] 全站搜索。

退出标准：已通过。文章与资料筛选状态可分享并恢复；主题、标签、统一归档、搜索和旧 URL 迁移贯通；真实长文覆盖表格、代码、公式与两张原创插图；69 个静态页面完成生产构建，桌面与 390px 浏览器流程无横向溢出。完整记录见 [内容页面与发现体验](doc/CONTENT_EXPERIENCE.md)。

### 阶段 5.1：信息架构与视觉重建修订（P0）—已完成

- [x] 以“流动手稿”取代 Apple 发布会/工程信号视觉，建立温暖、手工感、青春的视觉系统。
- [x] 自托管霞鹜文楷 Bright 与 EB Garamond，并保留 Noto Serif SC、JetBrains Mono 的正文/代码职责。
- [x] 首页改为知识轨迹，按内容总览、最新长文、最新便笺、资料分类、旅程与关于组织。
- [x] 文章一级栏目同时容纳长篇札记与灵光便笺。
- [x] 将 8 份授权 Markdown 原文按四类全部公开，只移除不可公开访问的本地图片绝对路径。
- [x] 旅程拆为教育、校园服务、获奖与里程碑，并扩充为 21 个经过确认或核验的坐标。
- [x] 关于页加入研究方向、关注领域、兴趣、学校、位置、GitHub 与 Motues 友联。
- [x] 删除 RSS、隐私、版权、源码、模板上游、独立友链页和旧 URL 兼容入口。
- [x] 完成桌面、390×844 手机、浅色、深色、移动菜单、分类筛选和原始正文视觉实测。
- [x] 修复主题切换在页面过渡后重复绑定的问题。

退出标准：36 个 Markdown 文件通过内容预检；96 个静态页面可构建；五项主导航、四类原始资料、三段旅程结构和关于页信息完整可达；控制台无运行错误。完整设计决策见 `doc/DESIGN_DIRECTION_V2.md`，累计实现见根目录 `README.md`。

### 阶段 6：CMS 与发布体验（3–6 天，P0）

- [ ] 扩展 CMS 支持新的内容类型。
- [ ] 加入字段校验、图片优化和预发布检查。
- [ ] 验证草稿不会进入生产站点。
- [ ] 编写日常发布 SOP。
- [ ] 为构建失败、冲突和回滚编写故障处理步骤。

退出标准：用 CMS 完整发布一篇真实内容，操作时间达到目标，线上结果正确。

### 阶段 7：质量与上线（2–4 天，P0）

- [ ] 完成自动检查和测试矩阵。
- [ ] 修复 Lighthouse、a11y、断链和布局问题。
- [ ] 校验 SEO、sitemap 和分享图，并确认 RSS 按用户要求保持移除。
- [ ] 完成公开数据边界和第三方服务审查，并确认不重新建立隐私/版权公开页。
- [ ] 在真机上执行全部核心流程。
- [ ] 清理调试日志、模板占位和未使用资源。
- [ ] 创建首发版本标签并记录线上基线。

退出标准：满足全站 Definition of Done。

### 阶段 8：首发后增强（持续，P1）

- [ ] 在线远程 Studio。
- [ ] 评论或反馈系统。
- [ ] 项目案例独立栏目。
- [ ] 自动生成分享图。
- [ ] 失效链接定期检测。
- [ ] 隐私友好统计。
- [ ] 多语言逐篇启用。
- [ ] 对象存储和媒体库。
- [ ] Webmention、newsletter 或内容联合发布（按真实需求选择）。

---

## 17. 首发内容清单

### 17.1 必备内容

- [ ] 关于我：我是谁、关注什么、为什么建立本站。
- [ ] 建站说明：技术选择、内容原则和未来计划。
- [ ] 代表性长文 1 篇。
- [ ] 学习笔记/教程至少 2 篇。
- [ ] 学习资料至少 5 条，覆盖 2 个主题和 2 种资源类型。
- [ ] 短想法至少 3 条，用于验证轻量内容样式。
- [ ] 经历至少 3 个节点。
- [ ] 隐私与版权说明。

### 17.2 每篇内容质量基线

- 解决一个具体问题或留下一个明确观察。
- 有作者自己的判断、实践或联系，不只是摘录。
- 有清楚来源和时间上下文。
- 有下一步行动或可继续探索的链接。
- 在未来六个月仍有回看价值；纯临时信息应明确时效。

---

## 18. 日常运营 SOP

### 18.1 快速记录

1. 想法先进入 Inbox，不要求完整。
2. 标记内容类型和一个主题。
3. 有来源立即保存原始链接和访问日期。
4. 涉及他人或隐私时默认标为 Private，并移出公开仓库。

### 18.2 每周整理（建议 30–60 分钟）

- 清理 Inbox，决定删除、保留、合并或发展为文章。
- 更新正在学习资料的进度与一句收获。
- 检查草稿是否缺少明确问题和结论。
- 选出下一篇最值得完成的内容，而不是同时铺开太多草稿。

### 18.3 每月维护

- 更新 Now 区域。
- 检查失效链接、过期内容和错误信息。
- 查看依赖安全更新和 Momo 上游变化。
- 检查构建时长、产物体积和 Lighthouse 趋势。
- 备份内容与媒体。
- 合并重复标签，处理孤立内容。

### 18.4 每季度回顾

- 哪些内容仍有价值，哪些应更新或归档。
- 哪些主题真正形成积累，哪些分类只是噪音。
- 发布流程中最耗时的一步是什么，是否值得自动化。
- 是否真的需要 P1 功能，如评论、远程 Studio 或统计。
- 隐私页、联系方式和个人经历是否仍准确。

---

## 19. 风险登记表

| 风险 | 概率 | 影响 | 预防/应对 |
|---|---|---|---|
| 过度追求视觉导致迟迟不能发布 | 高 | 高 | 先完成内容与组件基线；每阶段有明确退出标准 |
| Apple 式动效影响性能 | 中 | 高 | 性能预算、reduced-motion、只动画 transform/opacity |
| GitHub Pages 不支持在线写入 | 高 | 中 | P0 本地 CMS；P1 独立鉴权 Studio |
| 模板更新覆盖定制 | 中 | 高 | `upstream` 只读、独立分支评估、手工迁移视觉层 |
| 内容类型过多导致维护负担 | 中 | 中 | P0 可合并 thoughts/projects，按真实数量再拆分 |
| 个人隐私泄露 | 低/中 | 极高 | 分级、发布清单、密钥扫描、Private 不进仓库 |
| 图片使仓库膨胀 | 中 | 中 | 上传优化、体积预算、季度检查、必要时对象存储 |
| 评论带来垃圾信息与合规负担 | 中 | 中 | 默认关闭，有明确需求后再启用 |
| 中英文双语长期不同步 | 高 | 中 | 首发中文优先，英文逐篇显式启用 |
| 标签失控 | 中 | 中 | 受控词表、季度合并、CMS 自动建议 |
| 构建依赖未来不兼容 | 中 | 中 | 锁文件、CI、定期小步更新、保留可回滚版本 |

---

## 20. 决策日志

| 日期 | 决定 | 原因 | 后续复查 |
|---|---|---|---|
| 2026-09-09 | 以 Momo 26.8.15 为技术基线 | 已具备博客、搜索、RSS、i18n、本地 CMS 与 Pages 部署能力 | 大版本升级前复查 |
| 2026-09-09 | 保留 `origin` 为个人仓库，增加 `upstream` | 兼顾独立发布与模板更新 | 每月检查上游 |
| 2026-09-09 | P0 采用静态站点 + Git 内容 + 本地 CMS | 安全、低成本、可迁移，适合个人维护 | 发布频率达到门槛后评估远程 Studio |
| 2026-09-09 | 视觉参考 Apple 发布会语言但不复制品牌资产 | 获得聚焦和舞台感，同时保持原创性 | 设计系统完成后复查 |
| 2026-09-09 | 评论默认关闭直到自有方案完成 | 模板默认后端不属于本站，存在隐私和可靠性风险 | P1 再评估 |
| 2026-09-09 | 首版使用 Miles 署名与抽象 M 标识 | 使用可验证的公开身份，不推测姓名或发布未授权头像 | 本人提供新资料后复查 |
| 2026-09-09 | 首发中文优先，暂时关闭英文入口 | 避免在真实双语内容不足时增加长期同步成本 | 准备好首篇英文内容时复查 |
| 2026-09-09 | 友链首发不开放 | 没有经过确认的真实互相链接，避免保留模板作者信息 | 有真实条目时复查 |
| 2026-09-09 | Momo 示例内容改为内部归档 | 保留 Markdown 能力参考，同时保证公开网站不冒用模板作者内容 | 阶段 3 建立专用设计验证页 |
| 2026-09-10 | 公开两段由本人提供的教育经历 | 用户已明确提供学校与年份，足以形成可信时间线 | 专业和培养层次仅在本人授权后补充 |
| 2026-09-10 | 奖项仅发布经核验的脱敏文字摘要 | 证明材料可验证事件，但包含姓名、编号、队友和二维码等隐私 | 项目细节和个人职责需本人确认后再发布 |
| 2026-09-10 | 本地学习素材先盘点、后重写 | 笔记含外部教程、课程材料和可能过时的命令，原样导入存在版权与准确性风险 | 阶段 3–6 按内容类型逐项转换和复核 |
| 2026-09-10 | 设计系统优先使用系统字体与语义令牌 | 减少字体请求和临时样式，让舞台感来自层级、留白和内容 | 阶段 4 首页实装后校准字号与章节节奏 |
| 2026-09-10 | 保留 Momo 旧变量兼容映射 | 阶段 2 不应为了换视觉一次性破坏全部内容组件 | 阶段 5 页面迁移完成后评估移除别名 |
| 2026-09-10 | 设计验证页公开可访问但不进入导航和搜索 | 便于跨设备验收，不把内部工具当作正式内容 | 首发前决定是否保留线上路由 |
| 2026-09-10 | 按内容职责拆分五个公开 collection | 不让文章字段承担资料、短想法和经历的全部语义，便于独立检索与演进 | 有真实项目案例时评估独立 `projects` collection |
| 2026-09-10 | 主题、状态、类型、许可和可见性使用受控词表 | 防止自由输入造成同义词、非法状态和筛选漂移 | 每季度结合真实内容复查词表 |
| 2026-09-10 | 文章规范路径改为 `/articles/` 并保留旧 `/blog/` 迁移页 | 顶级 URL 与首发信息架构一致，同时避免早期公开链接失效 | 若引入边缘层则升级为 HTTP 301 |
| 2026-09-10 | 本地资料只转写原创导读并链接官方来源 | 降低版权、过时命令和隐私风险，不把个人站变成第三方文件镜像 | 深入教程发布前逐条实测与注明版本 |
| 2026-09-10 | 阶段 6 前暂停使用旧 CMS 保存内容 | 旧表单仍写入已迁移的 `blog` 模型，直接使用会产生无效内容 | 阶段 6 完成新类型表单和端到端发布 |
| 2026-09-10 | 首页采用八段线性叙事而非文章瀑布流 | 先回答身份、当前关注与精选内容，再展开资料、想法、经历和最新内容 | 内容量显著增长后复查首页长度 |
| 2026-09-10 | 首页主视觉只用自有 SVG 与 CSS | 保持原创、轻量和深浅主题一致，不依赖未授权个人照片或第三方品牌素材 | 获得可公开项目影像后评估替换精选区视觉 |
| 2026-09-10 | 主导航固定五项，辅助入口放页脚 | 降低首屏决策成本，同时保留短想法、归档、RSS 和隐私入口 | 用户行为数据可用后再评估顺序 |
| 2026-09-10 | 首页身份视觉改为工程“信号轨迹” | 将发布会式克制落到 Miles 的学习与验证语境，避免通用圆环、渐变字和无意义编号 | 有本人授权的工程影像后再评估扩展视觉素材 |
| 2026-09-10 | 取消 AOS 批量滚动淡入 | 内容无需等待动画即可阅读，降低前台脚本和模板化观感 | 仅保留一次性首屏编排与直接交互反馈 |
| 2026-09-10 | 筛选状态使用 URL query，搜索使用 Pagefind 本地索引 | 查询可分享、刷新可恢复，且无需将访客搜索词发送给第三方 | 内容量增长后检查筛选性能与搜索质量 |
| 2026-09-10 | 搜索结果写入内容类型、主题和日期元数据 | 让同名或跨内容类型结果可快速区分 | 阶段 7 用预设查询复核排序与完整性 |

---

## 21. 待确认输入

阶段 1 以不虚构个人事实为前提，对已提供材料作最小必要公开，对未提供项目保留可修订的执行默认值：

- [x] 站点显示名：首版使用 `Miles`。
- [x] 首页核心句：“持续学习，也持续留下些什么。”
- [x] 个人头像/照片：暂不公开，使用自有抽象字母 M 标识。
- [x] 首批 3–5 个学习主题：电子设计与嵌入式、AI 与机器学习、工程工具、语言学习、长期知识整理。
- [x] 个人经历：公开两段由用户提供的教育经历，以及三项由材料核验的脱敏成果；不从证明推测个人职责。
- [x] 联系方式和社交链接：只公开 GitHub `Miles-gift`。
- [x] 语言：首发只做简体中文，英文内容等有真实需求时再启用。
- [x] 友链：暂不保留主导航入口，页面仅保留未开放说明。
- [x] 评论：首发不启用。
- [x] 域名：当前使用 `miles-gift.github.io`，暂不假设已有独立域名。
- [x] 现有资料来源：已盘点授权的本地学习与评奖目录；Markdown 笔记作为重写输入，第三方课程和证明原件不进入公开仓库。

完整决策、内容盘点和日后待补充项见 [品牌与内容简报](doc/BRAND_AND_CONTENT_BRIEF.md) 与 [个人素材盘点与发布边界](doc/PERSONAL_CONTENT_INVENTORY.md)。

---

## 22. 下一步执行顺序

1. [x] 完成阶段 0：安装依赖、生产构建、预览与基线截图。
2. [x] 完成阶段 1：形成个人品牌与内容简报，替换公开模板身份与示例内容。
3. [x] 完成阶段 2：建立设计令牌、基础 UI 组件与设计验证页。
4. [x] 完成阶段 3：冻结首版内容模型并迁移首批真实内容。
5. [x] 完成阶段 4：首页与主导航。
6. [x] 完成阶段 5：完善文章、资料、想法、旅程、关于、筛选、聚合、归档和搜索页面。
7. [x] 完成阶段 5.1：按最新需求重做信息架构、视觉、真实资料与旅程。
8. [~] 扩展 CMS，贯通一次真实发布。
9. [ ] 完成质量、公开边界和部署验收后首发。

下一个实施任务为：**执行阶段 6，重建 CMS 的多内容类型表单、验证、媒体与预发布检查，并用真实草稿贯通保存、构建、提交、部署和回滚流程。**

---

## 23. 变更记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-09-09 | 建立项目愿景、内容架构、Apple 发布会式设计系统、技术方案、阶段计划、验收与运维规范 |
| v1.1 | 2026-09-09 | 完成阶段 0 本地基线：环境约束、依赖、博客/CMS 构建、生产预览、视觉截图、安全清理和发布工作流升级 |
| v1.2 | 2026-09-09 | 完成阶段 0 远端验证：修复 pnpm 版本冲突，GitHub Actions 构建与 Pages 部署成功，关键线上路径通过冒烟检查 |
| v1.3 | 2026-09-09 | 完成阶段 1：建立 Miles 品牌和中文内容基线，完成素材盘点、首发选题、隐私边界、About 和首篇内容，Momo 示例退出生产集 |
| v1.4 | 2026-09-10 | 补全阶段 1 个性化基线：加入教育时间线与三项脱敏成果，盘点授权目录中的学习材料，建立来源、版权和隐私分级 |
| v1.5 | 2026-09-10 | 完成阶段 2：建立语义设计令牌、五个基础组件、新导航与页脚、深浅主题和响应式设计验证页 |
| v1.6 | 2026-09-10 | 完成阶段 3：建立五个严格内容集合、受控 taxonomy 和构建前预检，迁移首批真实内容，贯通详情路由、搜索、RSS 与旧 URL 兼容页 |
| v1.7 | 2026-09-10 | 完成阶段 4：建立八段式数据驱动首页、自有 CSS 舞台主视觉、五项主导航、移动菜单与四个可用集合入口页 |
| v1.8 | 2026-09-10 | 完成阶段 5：建立文章/资料筛选、主题与标签聚合、统一归档、独立搜索、长文样例、相关内容、隐私/版权与 404，并按前端设计规范将首页收敛为工程“信号轨迹”视觉 |
| v1.9 | 2026-09-10 | 完成阶段 5.1：建立“流动手稿”视觉与知识轨迹首页，重组五大栏目，公开 8 份 Markdown 原文，扩充 21 个旅程坐标，并移除全部不再需要的公开入口 |
| v1.9.1 | 2026-09-10 | 修复阶段 5.1 干净环境构建：同步移除已下线 pages 集合的 schema 与预检引用，恢复 GitHub Pages 部署链路 |
