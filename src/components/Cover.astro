---
import "@styles/global.css"
import PageHero from "./ui/PageHero.astro";

interface Props {
    title?: string;
    subTitle?: string;
}

const { title, subTitle } = Astro.props;
---

{title && <PageHero title={title} description={subTitle} size="compact" />}
